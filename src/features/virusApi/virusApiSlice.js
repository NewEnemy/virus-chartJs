import {createSlice} from '@reduxjs/toolkit'

export const virusApiSlice = createSlice({
    name:'virusApi',
    initialState:{
        data:1
    },
    reducers:{},
}) 

export default virusApiSlice.reducer